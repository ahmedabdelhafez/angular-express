import { ReactivevalidationDirective } from './reactivevalidation.directive';

describe('ReactivevalidationDirective', () => {
  it('should create an instance', () => {
    const directive = new ReactivevalidationDirective();
    expect(directive).toBeTruthy();
  });
});
